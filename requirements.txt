fastapi==0.110.0
uvicorn[standard]==0.27.1
gunicorn==21.2.0

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Passlib 1.7.4 expects bcrypt.__about__, removed in bcrypt 4.1+.
bcrypt==4.0.1
python-multipart==0.0.9

# Config & Validation
# pydantic>=2.10 ships cp314 wheels; 2.6.4 has no Python 3.14 wheel.
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
email-validator==2.2.0

# Observability (optional but recommended)
loguru==0.7.2

# OpenTelemetry — OTLP export (no-op when OTEL_EXPORTER_OTLP_ENDPOINT is unset)
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=0.41b0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-instrumentation-pymongo>=0.41b0

# Async support (future-ready)
anyio==4.3.0
requests
httpx==0.27.2

# HTML Template transformer
jinja2==3.1.3

# Database
pymongo==4.10.1

# WebAuthn / FIDO2 biometric authentication
webauthn==2.1.0

# AI extraction
google-generativeai==0.8.5
Pillow==10.4.0

